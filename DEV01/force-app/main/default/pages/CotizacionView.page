<apex:page controller="CotizacionController">
	<apex:pageBlock >
        <apex:outputPanel >
        	<apex:pageMessages id="msg"/>
        </apex:outputPanel>
        
    	<apex:form >
        	<apex:pageBlockSection title="Buscar un producto" columns="1">
        		Código de Producto:
                <apex:inputText id="buscarProd" value="{! cod_prod }"/>
                <apex:commandButton Id="btnBuscar" action="{! buscarProducto }" rerender="renderBlock, msg" status="status" title="Buscar" value="Buscar" />
                
            </apex:pageBlockSection>
        </apex:form>
        
        <apex:form >
            <apex:pageBlocksection id="renderBlock" >
                <apex:pageBlockTable value="{! resultados }" var="i" rendered="{!NOT(ISNULL(resultados))}">
                    <apex:column value="{! i.Name }"/>
                    <apex:column value="{! i.IdExterno__c }"/>
                    <apex:column > <apex:facet name="header">Cantidad</apex:facet> <apex:inputField value="{! i.Cantidad_apart__c }" /></apex:column>
                    <apex:column value="{! i.Cantidad_dis__c }"/>
                </apex:pageBlockTable>
                
                <apex:pageBlockSection >
                	<apex:commandButton Id="btnIngresarCantidad" action="{! ingresarCantidad }" status="status" title="Ingresar Cantidad" value="Guardar" />
                </apex:pageBlockSection>
                
            </apex:pageBlocksection>
         </apex:form>
        
    </apex:pageBlock>>
</apex:page>